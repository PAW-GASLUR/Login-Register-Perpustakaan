@model IEnumerable<Perpustakaan.Models.Gender>

@{
    ViewData["Title"] = "Index";
}

<h2>Gender</h2>

<p>
    <a asp-action="Create">Tambah Data</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NoGender">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NoGender">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NoGender">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
