@model IEnumerable<Perpustakaan.Models.Rak>

@{
    ViewData["Title"] = "Index";
}

<h2>Rak</h2>

<p>
    <a asp-action="Create">Tambah Data</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaRak)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaRak)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NoRak">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NoRak">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NoRak">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
